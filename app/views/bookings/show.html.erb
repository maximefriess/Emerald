<%= render 'navbars/back_navbar' %>

  <div class="container inbox">
    <div id="analytics-filter-bar-show">
    <%= image_tag 'filter-icon.png' %>

  </div>
    <style>
    .hidden {
      display: none;
    }
  </style>
  <div class="hidden analytics-filter-menu" >
    <ul class="nav nav-tabs" role="tab-list">
      <li role="presentation" class="active"><a href="#month" id="tab" aria-controls="month" data-toggle="tab" role="tab"> Month</a></li>
      <li role="presentation"><a href="#year" id="tab" aria-controls="year" data-toggle="tab" role="tab"> Year</a></li>
    </ul>
    <div style="width: 100%" >
      <form action=" /bookings" method="get">
        <div role="tab-panel" class="tab-pane fade in" id="month">
          <% @months.each do |month| %>
            <div class="filter-category">
              <label for="month"><%= month %></label>
              <div class="round">
                <input name="month[]" value="<%= month %>" type="checkbox" id="<%= month %>" />
                <label for="<%= month %>"></label>
              </div>
            </div>
          <% end %>
        </div>
        <div role="tab-panel" class="tab-pane fade in" id="year">
          <% @years.each do |year| %>
            <div class="filter-category">
              <label for="year"><%= year %></label>
              <div class="round">
                <input name="year[]" value="<%= year %>" type="checkbox" id="<%= year %>" />
                <label for="<%= year %>"></label>
              </div>
            </div>
          <% end %>
        </div>
        <input type="submit" value="OK" class="inbox-btn-yellow" />
    </div>

  </div>
    <%= render 'shared/analytics_card' %>
  </div>

<%= render 'shared/footer' %>
